# Brand Assets

- `meettranscript-logo.svg`: primary horizontal logo
- `meettranscript-mark.svg`: square icon mark
- `MeetTranscript.icns`: macOS app icon (generated)

Generate the app icon bundle:

```bash
./scripts/generate_app_icon.sh
```

This script creates:

- `Brand/AppIcon.iconset/`
- `Brand/MeetTranscript.icns`
